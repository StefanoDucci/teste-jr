Informações que podem ser relevantes:

- A API foi desenvolvida no Laravel 5.4

- A factory criada responde a chamada: factory('App\Venda', 30)->create();

- Foi utilizada a ferramenta POSTMAN para testar a API durante o desenvolvimento.
